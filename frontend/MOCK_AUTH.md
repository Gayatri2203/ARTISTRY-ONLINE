# Mock Authentication (removed)

Mock authentication has been removed from the codebase. The frontend now calls real API endpoints (e.g., /api/auth/*). Remove this file when backend endpoints are implemented or keep for historical reference.
