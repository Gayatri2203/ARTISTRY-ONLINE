import { SignupPage } from "@/src/sections/auth";

export const metadata = {
  title: "Create Account | Artistry Online",
  description: "Join Artistry Online — the premium art marketplace.",
};

export default function SignupRoute() {
  return <SignupPage />;
}
