"use client";

import { AuthPageLayout, LoginForm } from "@/src/features/auth";

export default function LoginPage() {
  return (
    <AuthPageLayout>
      <LoginForm />
    </AuthPageLayout>
  );
}
