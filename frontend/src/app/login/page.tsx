import { LoginPage } from "@/src/sections/auth";

export const metadata = {
  title: "Sign In | Artistry Online",
  description: "Sign in to your Artistry Online account.",
};

export default function LoginRoute() {
  return <LoginPage />;
}
